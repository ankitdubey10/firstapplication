import React from "react";
function Reuse(props)
{
    return(
        <div style={{backgroundColor:"lightgrey"}}>
                <span>{props.sentData.name}</span>
        </div>
    )
}  
export default Reuse; 